[build-system]
requires =  ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name =  "voyage"
version = "0.1.0"
description = "version control but for tasks , projects , books and goals"
requires-python = ">= 3.11"

[project.scripts]
voy = "voyage.cli:main"